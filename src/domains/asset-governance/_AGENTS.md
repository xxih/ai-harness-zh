<!-- skill: learning-capture -->
- 运行中遇到用户的明确纠正、接受 / 拒绝标准、长期偏好或“以后都这样做”的口径，可视为反馈信号候选。
- 只记录用户明确表达的信号；不要根据语气、情绪或一次性抱怨脑补长期规则。
- 项目级 / 团队级 / 分发级的长期规则候选优先进入 `.learned/rules.md`；其他可复用经验进入 `.learned/notes.md`。
- 若当前任务采用 `nanospec/` 容器，且这些信号影响范围、方案或后续动作，要同步更新 `alignment.md` 与 `outputs/3-tasks.md`。
- 除非用户明确要求顺手改正式文档，否则先排队，不直接改写 `AGENTS.md`、README、skill 或 command 正文。
<!-- /skill: learning-capture -->
