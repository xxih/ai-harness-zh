# Alignment Log

## 2026-03-18

- [变更] 用户要求为 `learning-capture` 增加一层 `AGENTS.md` 协同：利用默认注入上下文，在运行过程中捕获“用户明确反馈信号”，把公共规则候选半自动排队。
- [变更] 本次方案允许“注入式 AGENTS + 手工整理”的半自动沉淀，但仍不引入 hooks、observer、后台扫描或自动发布正式文档。

## 2026-03-19

- [变更] 用户将本轮范围收窄为“新增领域级 `_AGENTS.md` 全局规则”，不再要求逐个改 `learning-capture` 等 skill 正文。
- [偏差] 上一版把 `_AGENTS.md` 错定义成“领域通用上下文层”，并引入了 `# ...`、`## 通用规则` 这类额外壳子；这与用户意图不符。
- [变更] `src/domains/<domain>/_AGENTS.md` 的语义修正为“领域根目录下的载体文件”：更像一个容器，用来存放某些 skill / command 需要默认注入的搭配上下文，而不是领域总说明。
- [变更] `_AGENTS.md` 的分隔机制修正为纯分隔头：每个块使用 `--- skill: <name>` 或 `--- command: <name>` 开头，不再要求 `## 通用规则`、标题或领域导言。
- [变更] 由于多数工具默认只自动读取 `AGENTS.md`，本轮只补充源资产与分发说明中的映射口径，不扩展为自动合并脚本。
- [变更] 用户进一步要求：当前仓库自有资产中，除根目录 `AGENTS.md` 外，禁止新增或保留其他 `AGENTS.md`，包括 `targets/`；若某个目标工具运行时需要该文件，当前阶段采用仓库外手动复制、手动改名。
- [变更] 用户随后澄清：`.nanospec/AGENTS.md` 可以保留；`targets/` 若要承载这类内容，不使用 `AGENTS.md`，而是统一改为 `_AGENTS.md`。
- [变更] 用户进一步要求：`_AGENTS.md` 的分隔方式改用 XML 注释标签包裹，而不是 `--- skill: ...` 这种单行分隔头。
- [变更] 用户进一步要求：`_AGENTS.md` 的正文必须把前因后果写清楚，并且能脱离 skill 正文独立生效；需要简洁明确地写出作用、记录条件、写入位置和默认动作，不能再用“排队”这类含混措辞。
- [变更] 用户进一步要求：XML 注释头统一改为 `AGENTS: xxx`；示例正文不再提 `learning-capture`、`nanospec`、`align` 或“独立生效”这类空话。除 `nanospec` / `spec-driven` 或用户明确要求的场景外，prompt 默认保持独立，不互相呼应。
