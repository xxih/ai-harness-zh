## 语言

- 该仓库默认使用简体中文
- 新增或修改仓库内文档、skill、command、eval 定义时，默认优先使用中文
- 代码、路径、API 名称、协议关键字保持原文

## 工作规则

- 共享资产默认保持工具无关，只有目标平台明确绑定时才写入平台细节
- 涉及结构性变更后，同步更新对应说明文档，避免仓库约定失真
- 外部参考仓库统一放在 `references/repos/`，该目录供 AI 读取，但不纳入当前仓库 git 管理

## 反馈信号沉淀

- 若用户在运行过程中给出明确纠正、接受/拒绝标准、长期偏好或“以后都这样做”的口径，视为可沉淀的反馈信号
- 项目级 / 团队级 / 分发级的长期规则候选优先记录到 `.learned/rules.md`；其他可复用经验记录到 `.learned/notes.md`
- 只记录用户明确表达的信号，不根据语气、情绪或单次偶发措辞自行脑补长期规则
- 若当前任务采用 `nanospec/` 容器，且该反馈信号影响范围、方案或后续动作，要同步更新 `alignment.md` 与 `outputs/3-tasks.md`
- 除非用户明确要求，先排队候选，不直接改写正式共享文档

## Commit 规范

使用简单单行格式：

`<type>: <summary>`

推荐类型：

- `feat`：新增资产或新增能力
- `fix`：修复问题或修复校验
- `docs`：文档或 prompt 文案更新
- `refactor`：重构但不改变预期行为
- `test`：补充或调整验证样例、检查步骤或测试材料
- `chore`：仓库维护

补充约定：

- `summary` 默认使用简洁中文
- 一个 commit 只做一类相对聚焦的改动
- 除非是仓库级变更，否则不要把无关资产混在同一个 commit
