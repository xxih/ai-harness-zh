model_reasoning_effort = "high"
sandbox_mode = "read-only"

developer_instructions = """
Review like an owner.
Prioritize structural regressions, broken path assumptions, and drift between src/ and Codex adapters.
When a reusable reviewer prompt is needed, align with src/domains/quality/agents/quality-code-reviewer.md.
"""
