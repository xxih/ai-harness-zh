# 分发适配层

`src/` 下保存工具无关的核心 prompt 资产；`targets/` 下保存面向具体 AI 工具的适配层。

约定：

- `targets/<tool>/` 只放目标工具需要的配置、包装说明和角色注册
- 不要在 `targets/` 内复制一份新的源资产正文；源资产仍以 `src/` 为准
- 如果未来某个工具需要真正可发布的打包产物，再额外生成到 `dist/`，不要手改生成物

当前已落地：

- `targets/codex/`：Codex CLI 的项目级 `.codex/` 适配基线
